var directionFactory = require(process.cwd() + '/script/direction-module'),
    directionHelper = require(process.cwd() + '/test/spec/helpers/direction-module.helper');

describe('prediction request non-proximity', function() {
  
  it('should notify led.off()', function() {

  });

});